# Kanopy security and ownership (requires web-app chart v4.30.0+)
security:
  dataClassification: "Internal"
  dataDesignation: ["MI"]

ownership:
  driEmail: florencia.arin@mongodb.com

env:
  NODE_ENV: prod
  # AWS region for IRSA (no credentials needed)
  AWS_REGION: us-east-1
  # Backend API URL for frontend to connect to backend health check
  BACKEND_URL: http://localhost:8000 # if multicontainer pod is setup and backend is running on 8000

## Single Pod Containers
envSecrets:
  # MongoDB Configuration
  MONGODB_URI: retail-supply-chain-management-prod
  DATABASE_NAME: retail-supply-chain-management-prod
  ATLAS_CHARTS_BASE_URL: retail-supply-chain-management-prod
  ATLAS_DASHBOARD_ID: retail-supply-chain-management-prod

## should be used in case of multi-pod container i.e: both frontend and backend in one pod
sidecarContainers:
  - name: backend
    image: 275662791714.dkr.ecr.us-east-1.amazonaws.com/retail-supply-chain-management-backend:latest

    ports:
      - containerPort: 8000
        name: backend
    env:
      - name: APP_NAME
        value: retail-supply-chain-risk

      # MongoDB connection (from Kubernetes secret)
      - name: MONGODB_URI
        valueFrom:
          secretKeyRef:
            name: retail-supply-chain-management-prod
            key: MONGODB_URI

      - name: DATABASE_NAME
        valueFrom:
          secretKeyRef:
            name: retail-supply-chain-management-prod
            key: DATABASE_NAME

      # Voyage AI API Key (embeddings)
      - name: VOYAGE_API_KEY
        valueFrom:
          secretKeyRef:
            name: retail-supply-chain-management-prod
            key: VOYAGE_API_KEY

      - name: LLM_API_KEY
        valueFrom:
          secretKeyRef:
            name: retail-supply-chain-management-prod
            key: LLM_API_KEY

      - name: LLM_BASE_URL
        valueFrom:
          secretKeyRef:
            name: retail-supply-chain-management-prod
            key: LLM_BASE_URL            

      - name: ANTHROPIC_MODEL
        value: claude-opus-4-7   

## ----------------------------- Cloud Specific Configurations (For AWS only line 42 to 46 needed)-----------------------------

## Add IRSA configuration here, this is for AWS
serviceAccount:
  enabled: true
  irsa:
    accountId: "275662791714"
    roleName: "kanopy-prod-cicd-irsa"

## For GCP this, example setup here is for manufacturing

# serviceAccount:
#  enabled: true
#  name: <service-account> # commit and helm should create Service account
# ## kubectl get pods -n ${KSA_NAMESPACE} -o jsonpath='{.items[0].spec.serviceAccountName}' | grep <demo-pod-name>

# # Mount GCP credential config and service account token
# extraVolumes:
#  - name: gcp-creds
#    configMap:
#      name: <config-map-name>
#  - name: gcp-token
#    projected:
#      sources:
#      - serviceAccountToken:
#          path: token
#          expirationSeconds: 3600
#          audience: "//iam.googleapis.com/projects/<USER_PROJECT_NUMBER>/locations/global/workloadIdentityPools/<WIF_POOL_ID>/providers/<WIF_PROVIDER_ID>"

# extraVolumeMounts:
#  - name: gcp-creds
#    mountPath: /etc/gcp
#    readOnly: true
#  - name: gcp-token
#    mountPath: /var/run/service-account
#    readOnly: true
